---
"@hiraoku/react-grab-copilot": minor
---

Add GitHub Copilot CLI provider for React Grab

- New package `@hiraoku/react-grab-copilot` for GitHub Copilot CLI integration
- Server runs on port 10567
- Supports `--allow-all-tools` and `--allow-all-paths` flags
- Streams stdout as plain text status messages
